# News Text Classifier (NLP)

This project classifies news headlines into categories using TF-IDF + Neural Network.

## Train the Model
Open `train_classifier.ipynb` and run all cells.

## Run the App
